/**
 * Application layouts.
 *
 * Placeholder — add layout wrappers here as the app grows:
 * - `auth-layout.tsx`  — centered card layout for login/register
 * - `app-layout.tsx`   — sidebar + topbar layout for authenticated pages
 * - `blank-layout.tsx` — minimal layout for error pages
 */
export {};
