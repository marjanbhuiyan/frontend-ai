/**
 * Global state store — placeholder.
 *
 * Add state management here when needed (e.g., Zustand, Jotai, Redux):
 * - `use-app-store.ts`   — global app state (sidebar open, modals, etc.)
 * - `use-theme-store.ts` — theme preferences
 * - `use-notification-store.ts` — toast / notification queue
 */
export {};
